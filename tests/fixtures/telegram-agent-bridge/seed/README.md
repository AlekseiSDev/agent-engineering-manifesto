# Telegram Agent Bridge

Prototype repository for developing an integration between AI coding agents and Telegram.

Current shape:

- a Telegram bot receives commands and project links;
- an agent bridge turns chat requests into local coding tasks;
- status updates flow back to Telegram;
- the repo is early-stage and has no agent docs yet.

The team wants a setup that helps future agents work safely around integration boundaries without creating a huge docs tree.
